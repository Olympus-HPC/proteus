[build-system]
requires = [
  "setuptools>=69",
  "setuptools-scm>=8",
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]

[tool.cibuildwheel]
build = "cp310-* cp311-* cp312-* cp313-* cp314-*"
skip = "*-musllinux_* cp31?t-*"
